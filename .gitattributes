# 统一换行符，避免 Windows / macOS / Linux 协作时产生整文件 diff
* text=auto eol=lf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary

# 锁文件在合并时保留双方记录，避免冲突后需要重新安装
pnpm-lock.yaml merge=binary
